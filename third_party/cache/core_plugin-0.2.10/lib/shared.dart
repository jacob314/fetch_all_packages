import 'package:core_plugin/user/user_info.dart';

class Shared {
  static UserInfo currentUser;
  static CheckoutInfo currentCheckoutInfo;
  static DeviceInfo currentDeviceInfo;
  static ConfigInfo currentConfigInfo;
}
