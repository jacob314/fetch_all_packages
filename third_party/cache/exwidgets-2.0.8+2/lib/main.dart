import 'package:exwidgets/demo.dart';
import 'package:flutter/material.dart';

void main() {
  //Injector.configure(Flavor.MOCK);
  // FirebaseLib.init();
  // FirebaseLib.activateNotification();
  // FlutterBackgroundGeoLocation.initBackgroundGeoLocation();

  runApp(DemoScreen());
}
